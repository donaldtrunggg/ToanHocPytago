{% set environment = app.environment %}
{% dump(asset_url) %}
{% if environment == 'dev' %}
    <link rel="stylesheet" href="{{ asset_url }}?version={{ assets_version }}" />
{% else %}
    <link rel="stylesheet" href="/wow-cv{{ asset_url }}?version={{ assets_version }}" />
{% endif %}